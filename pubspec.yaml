name: sikkim_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 📱 UI & Icons
  provider: ^6.1.2
  flutter_spinkit: ^5.2.1
  intl: ^0.19.0

  # 📍 Location services
  # 🔊 Audio

  # 🖼️ Image picking & 3D models

  # 📝 ML Kit - Text recognition, translation & language ID
  google_mlkit_text_recognition: ^0.12.0
  google_mlkit_translation: ^0.10.0
  google_mlkit_language_id: ^0.13.0

  # 🤖 AI - Gemini
  google_generative_ai: ^0.4.7

  # 🌐 Networking
  http: ^1.1.1

  # 🔑 Permissions (Camera, Media, etc.)
  permission_handler: ^11.4.0

  # UI
  cupertino_icons: ^1.0.8
  model_viewer_plus: ^1.9.3
  audioplayers: ^6.5.1

  # Location
  geolocator: ^14.0.2
  geocoding: ^4.0.0

  # Firebase
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  firebase_storage: ^13.0.1

  # Image picking
  image_picker: ^0.8.7+4

  # HTTP requests
  http: ^1.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/vehera_vihara_-_stupa_scene.glb
    - assets/manuscripts/

    - assets/audio/sculpture1.mp3
    - assets/audio/sculpture2.mp3
    - assets/audio/sculpture3.mp3
    - assets/audio/sculpture4.mp3
    - assets/models/sikkim_monastery_lowpoly.glb
    - assets/images/heritage_bg.jpg

